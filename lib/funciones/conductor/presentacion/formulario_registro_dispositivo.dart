import 'package:flutter/material.dart';

import 'pagina_conductor.dart';

class FormularioRegistroDispositivo extends StatelessWidget {
  const FormularioRegistroDispositivo({super.key});

  @override
  Widget build(BuildContext context) {
    return const PaginaConductor();
  }
}
